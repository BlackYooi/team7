<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <resultMap id="UserMap" type="com.example.demo.Bean.User">
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="sex" jdbcType="VARCHAR" property="sex"/>
        <result column="age" jdbcType="VARCHAR" property="age"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="id" jdbcType="VARCHAR" property="id"/>
    </resultMap>

    <select id="findAll" resultMap="UserMap">
        select * from user
    </select>

    <insert id="addUser" parameterType="com.example.demo.Bean.User">
        insert into user (name,sex,age,password,id) values (#{user.name},#{user.sex},#{user.age},#{user.password},#{user.id})
    </insert>

    <update id="updateUser" parameterType="com.example.demo.Bean.User">
        update user set user.name=#{user.name},sex=#{user.sex},password=#{user.password},age=#{user.age} where id=#{user.id}
    </update>

    <delete id="deleteUser" parameterType="com.example.demo.Bean.User">
        delete from user where id=#{user.id}
    </delete>


</mapper>